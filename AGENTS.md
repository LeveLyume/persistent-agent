# 项目开发协作规范

## 1. 角色与权限

Owner 决定产品目标、开发版本、重大架构方向和重大技术取舍。
Codex 负责理解仓库、设计实现、拆解任务、编码、验证、自审、维护文档、提交推送和汇报。

Owner 决定 What / Why，Codex 负责授权范围内的 How。

权限分为三级：

- 自主决定：命名、函数拆分、局部数据结构、异常处理、测试组织、格式及小型局部重构。
- 实施后说明：新增内部模块、调整内部接口或局部目录、增加必要依赖、非破坏性 schema 调整及任务所需的有限重构。须在汇报和 work-log 中说明。
- Owner 决策：改变版本目标或整体架构、替换核心技术栈、修改重要公开 API 或核心模块边界、删除重要能力、大规模重构，以及超出批准范围的产品行为变更。

遇到需要 Owner 决策的事项，暂停相关实施，说明原因和建议；不受影响的已授权工作可以继续。

## 2. 开发原则

- 先检查相关代码，再设计和修改；不凭文件名、README 或旧聊天猜测实现。
- 优先复用现有架构，不创建重复的 Runtime、Memory、Provider、数据模型或平行实现。
- 理解真实目标；发现具体方案存在架构、安全、数据或维护问题时，应指出并提出替代方案，不擅自改变产品目标。
- 采用最小充分修改：完整满足当前需求，保留必要设计，不提前实现未来功能。
- 无关技术债和优化默认只记录；仅处理当前任务必需、阻塞任务或与任务直接相关且低风险的问题。
- 每次提交的 Git 快照都应自洽：已实现能力、版本状态、使用说明、设计说明与工作记录不能互相矛盾；不要求机械修改所有文件，只更新受实际变更影响的内容。

## 3. 版本规划与授权

采用两级开发版本：

- Development Version：`v0.X`，由 Owner 创建，定义本轮目标和范围。
- Task Version：`v0.X.Y`，由 Codex 拆解，每项应是小型、完整、可独立验证的工程单元，不按文件或函数机械拆分。

开始新 Development Version 时：

1. 检查仓库并理解目标。
2. 提交 Task Plan，包含最后的 Version Finalization 任务。
3. 等待 Owner 一次性批准。

已完成 Development Version 的文档、协作规范等维护工作，若不改变产品目标、版本范围或整体架构，可按 Owner 的具体请求分配后续 `v0.X.Y` 维护任务；无需据此开启下一 Development Version 或重新审批整版 Task Plan。该维护任务不追溯改变已完成版本的状态；改变版本目标仍须由 Owner 决定。

Task Plan 批准后，Codex 可连续完成当前任务的实现、验证、文档和 commit；每次 push 前必须先汇报并等待 Owner 明确批准。计划批准不等于推送批准，未获批准不得 push。

允许在不改变版本目标的前提下调整、拆分或追加任务，须在 work-log 记录原因。改变版本目标或开启下一 Development Version 必须由 Owner 决定。

这些编号仅用于开发管理，不自动触发 Git Tag、Release、包版本修改或正式发布；发布操作须由 Owner 明确授权。

## 4. 任务执行与完成标准

每个 Task Version 按顺序完成：

检查仓库与相关代码 → 明确任务范围 → 实现 → 验证 → 自审
→ 同步受影响文档与 work-log → 项目一致性检查 → 检查暂存快照 → Commit
→ 推送前汇报 → 等待 Owner 批准 → Push → 核验远端与工作区 → 汇报结果 → 下一任务

Owner 要求先查看 diff 时，可在自审后停于“待审阅”：先汇报 diff 摘要、验证结果和未同步事项，不提前 commit 或 push。收到继续提交的指示后，先补齐受影响文档与 work-log，再完成一致性检查和 commit；push 仍须单独汇报并取得明确批准。待审阅不等于任务完成。等待审阅期间可处理 Owner 后续授权且互不依赖的任务，但必须隔离变更与提交；若待审阅修改会影响新提交的一致性，先解决该依赖。

每次 commit 前执行项目一致性检查：

1. 以代码、验证结果和本次 diff 为依据，检查 README 的用户能力、安装、配置、命令与限制是否准确。
2. 检查受影响的架构、专项设计、Roadmap、图示和示例；区分已实现、待核验、规划中，核对路径、链接、版本编号与任务状态。
3. 最后更新 work-log，记录实际变更、验证及限制、文档同步情况和预定 commit message；不预写未发生的 commit 或 push。
4. 检查 `git status`、工作区 diff 和暂存 diff，确认暂存快照包含本任务全部必要修改，没有无关文件、秘密或与未暂存修改相冲突的项目事实。
5. 只在上述检查通过后 commit。若必要同步超出 Owner 明确限定的文件范围，报告冲突并等待范围决定；不得为赶进度提交已知不一致的快照。

完成要求：

- 满足需求，遵守现有架构，无无关修改或越界开发。
- 执行与变更匹配的可靠验证，例如单元测试、集成测试、静态检查、CLI 冒烟测试或手动复现。
- 自审最终 diff，检查重复实现、错误路径、回归风险、敏感信息和文档一致性。
- 必要检查通过后，仅因新修改、失败或未解决问题追加验证。
- 受影响文档和 work-log 已同步，提交快照一致且符合规范，经过 Owner 批准后 push 成功；范围限制导致暂不能同步时，不 commit 或宣称 Task Version 完成。
- 每次涉及文件修改的任务（包括仅修改文档或协作规范）都必须以 commit 和 push 闭环；等待审批、推送失败或仅保存在本地时，不得宣布完成。
- Task Version（小版本）以对应 commit 和 push 成功为完成标记；可提前分配编号用于计划和提交，但不能因文件已修改或本地 commit 已生成就标记完成。

不得以“代码看起来正确”或“文件已存在”代替验证。测试条件不足时，说明实际验证范围和限制。

验证失败时先定位、修复并重新验证，不得标记完成或将已知损坏状态作为正常完成版本推送。
遇到无法解决的阻塞，应如实说明；当前任务未完成前不自行进入下一任务。Owner 后续授权的独立任务按上述待审阅规则处理。

## 5. Git 规则

默认：一个 Task Version = 一个原子提交 = 一次推送。
若任务包含多个独立变更，应优先拆分任务。

提交格式：

`<type>(<scope>): <description> [v0.X.Y]`

type 使用 `feat`、`fix`、`refactor`、`test`、`docs` 或 `chore`。
描述应说明实际变化，避免 `update`、`changes` 等模糊信息。

提交前检查 `git status`、`git diff` 和 `git diff --cached`；按路径暂存并复查暂存文件清单及完整 diff，不使用 `git add .` 代替范围判断。
推送前核对目标提交、目标分支和工作区状态，确保仅推送已汇报的提交范围；推送后核验远端分支指向目标提交，并列出仍未提交的工作区修改。
向 Owner 报告小版本号、文件范围、实际变更、验证结果与限制、commit message / SHA、目标远端与分支，并等待明确批准。批准后仅推送已汇报的提交范围；若范围有实质变化，重新汇报审批。

必须遵守：

- 不覆盖、删除或擅自提交 Owner 的无关修改。
- Owner 明确限定文件范围时先遵守范围；若与必要的文档同步冲突，在汇报中列出待同步文件和原因，不擅自扩大修改，也不提交已知不一致的项目快照。
- 存在用户修改时，尽量安全隔离自己的变更；无法隔离则报告阻塞。
- 不提交 secret 或真实 `.env`。
- 不强制推送、不改写历史。
- 不用 `git reset --hard` 或 `git clean -fd` 清理未知修改。

提交或推送失败时，如实报告状态，不宣称任务完成。

## 6. 文档与工作记录

文档应反映实际实现，只更新真正受影响的内容。每次提交前检查项目公开说明、相关设计和版本记录是否与本次提交后的状态一致；不受影响的文档在 work-log 中说明无需更新。

### docs/work-log.md

每个 Task Version 必须更新，并与代码一起提交。若 Owner 明确要求先审阅 diff 或限定文件范围而暂不允许更新，先在汇报中列出待同步事项；获得继续提交及相应范围的授权后补齐，未补齐前不 commit。未补齐时保持“待审阅”或“进行中”，不标记任务完成。

Development Version 记录：

- 版本名称、目标、状态。
- Task Plan 及完成勾选。
- 计划调整及原因。

Task Version 记录：

- 目标和实际变更。
- 重要工程决策。
- 验证结果及限制。
- README、设计文档的更新情况，或无需更新的说明。
- Commit message。
- 未解决问题和下一步（如有）。

不写逐分钟流水账，不删除旧历史，不把计划或未验证功能写成已完成。
在实现、验证和其他文档核对后，最后编辑 work-log；可以将已完成的工作日志更新写为“更新完成”。这里的“更新完成”只描述文档工作，不等于 Task Version 已完成，也不表示 commit 或 push 已发生。
通过版本号和 commit message 关联 Git，不为写入当前提交自身的 SHA 制造额外提交。
工作日志无需预写“待 commit / push”，也不得把尚未发生的 commit / push 写成已完成。工作日志编辑后，只进行必要的最终 diff 检查、commit、推送前汇报和审批；获得 Owner 批准后直接 push，再核验并汇报结果，不穿插无关修改或操作。推送结果在任务汇报中记录，后续有文件修改时再同步日志；不为回填 push 成功单独制造递归提交。

### README

描述项目当前对用户公开的事实。
用户能力、安装、启动、配置、CLI、运行依赖、平台支持或功能状态变化时更新。
纯内部重构通常无需更新，不把 README 写成开发日志。

### 设计文档与 Roadmap

已确定的设计发生变化时，同步相关文档。
仅按既有设计实现时，无需机械重写。
Roadmap 仅在实际受影响时更新。

## 7. 版本收尾

每个 Development Version 的最后一项必须是 Version Finalization：

- 运行覆盖当前版本目标的完整验证。
- 检查所有任务状态，修复遗漏和误报。
- 核对 README、相关设计文档及 work-log。
- 完成收尾提交，汇报并取得 Owner 的推送批准，再推送并核验；推送成功前保持待完成状态。

只有所有任务完成、验证通过、文档准确且收尾提交已推送，
才能对外宣布 `v0.X — COMPLETED`。

版本完成后交还控制权，等待 Owner 的下一版本规划，不自行开启新版本。

## 8. 汇报

每次 push 前先报告并请求批准：

- 小版本号、变更文件与实际内容。
- 验证结果、限制和文档更新。
- 待推送的 commit message / SHA、远端及分支。

Owner 明确批准后执行 push，核验成功后再报告任务完成：

- 版本号与实际变更。
- 重要决策。
- 验证结果和限制。
- 文档更新。
- Commit message 与 push 结果。
- 下一任务或阻塞。

推送前汇报是明确的审批节点，必须等待 Owner 批准；推送成功后的结果汇报无需重复请求同一次推送的批准。
Development Version 完成后提供一次整体总结，无需重复全部历史。
